import * as ActionType from './actionType'

const initialState = {
    user : {
        email: ""
    }
}
export default function update (state = initialState, action){
    switch(action.type){
        default:
            return state
    }
}